runtime: python312
service: default

env_variables:
  PYTHON_ENV: production

handlers:
- url: /static
  static_dir: static

- url: /.*
  script: auto

automatic_scaling:
  target_cpu_utilization: 0.65
  min_instances: 1
  max_instances: 3
